class ProfilesController < ApplicationController

  def index
  end

  # def show
  #   @user = @user_profile
  # end

  # private

  # def profile_params
  #   params.require(:user).permit(:name, :nickname, :age, :birth_day, :breed, :weight, :birth_place, :raise, :personality, :charm_point, :encounter).merge(user_id: current_user.id)
  # end
end
